"use strict"; //treat all js code as newer javascript version. Now it is inbuilt but can be added as well.
let name="Amish Verma"
let age=21
let isLoggedIn=false
let nu=null
let un=undefined
//number
// bigint
// String
// Boolean=>true/false
// null=> standalone value
//undefined=>value is not assigned yet
// symbol=>unique
// object
console.log(typeof(name));
console.log("null data type: "+typeof(nu));
console.log("undefined data type: "+typeof(un));
